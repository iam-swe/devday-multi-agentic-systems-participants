[project]
name = "conditional-routing"
version = "0.1.0"
description = "Langgraph Conditional Routing Example"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain-google-genai>=2.0.10",
    "langgraph>=0.2.74",
    "python-dotenv>=1.0.1",
]
